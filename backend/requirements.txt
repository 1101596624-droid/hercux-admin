# ============================================
# HERCU Admin Backend - Requirements
# ============================================
# 部署后请运行: python scripts/verify_dependencies.py
# 确保所有依赖正确安装
# ============================================

# FastAPI Core
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.27.0
aiosqlite>=0.17.0
alembic>=1.8.0

# Redis
redis>=4.0.0
hiredis>=2.0.0

# Neo4j
neo4j>=5.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.5
bcrypt==4.0.1  # 固定版本，避免兼容性问题

# AI Integration
anthropic>=0.7.0
openai>=0.27.0
httpx>=0.23.0
certifi>=2023.0.0

# AWS (可选)
boto3>=1.26.0

# Utilities
python-dotenv>=0.20.0
email-validator>=1.3.0
Pillow>=9.0.0
aiohttp>=3.8.0

# ============================================
# Document Text Extraction (关键依赖!)
# 这些库用于 Studio 文件上传功能
# 缺失会导致文件上传 500 错误
# ============================================
pypdf>=3.0.0           # PDF 解析
python-docx>=0.8.11    # Word (.docx) 解析
beautifulsoup4>=4.11.0 # HTML 解析 (EPUB/HTML 都需要)
EbookLib>=0.18         # EPUB 电子书解析
lxml>=4.9.0            # XML/HTML 解析加速
